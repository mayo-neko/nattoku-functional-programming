object Main {
    def main(args: Array[String]): Unit = {
        println(wordScore("re"))
    }
    // 1.11 practice
    def increment(x: Int): Int = {
        x+ 1
    }
    def getFirstCharacter(s: String): Char = {
        s.charAt(0)
    }
    def wordScore(word: String): Int = {
        word.length()
    }
}
